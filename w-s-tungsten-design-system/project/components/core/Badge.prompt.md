Small square tag for categories or statuses, in brand tones.

```jsx
<Badge tone="primary">Ferro Tungsten</Badge>
<Badge tone="accent">ICP-OES</Badge>
```

Tones: `neutral`, `primary` (teal), `accent` (ore-gold), `success` (mint), `solid` (filled teal).
