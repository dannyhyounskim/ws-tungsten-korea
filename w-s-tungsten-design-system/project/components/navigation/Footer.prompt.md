Ink site footer (scheme-4): wordmark, link row, hairline divider, centered legal line.

```jsx
<Footer logoSrc="assets/logo/logo-light.png" />
```

Pass `links` and `legal` to customize. `logoSrc` falls back to a "WSTK" wordmark.
